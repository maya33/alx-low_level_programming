#include "main.h"

/**
 * _strchr - using string char*
 * @c: some characters 
 * Return: 0 or 1
 */
char *_strchr(char *s, char c);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}

