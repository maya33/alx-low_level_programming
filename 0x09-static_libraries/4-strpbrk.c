#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.c>
#include <string.h>
/**
 * _strpbrk - using string to break some code
 * @c: some characters 
 * Return: 0
 */

char *_strpbrk(char *s, char *accept);
{
      return strpbrk(s, accept);
}
