#include "main.h"

/**
 * _isdigit - using digital programs
 * @c: some characters 
 * Return: 0 or 1
 */
int _isdigit(int c);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
