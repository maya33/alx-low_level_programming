#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.c>
#include <string.h>
/**
 * _isdigit - using digital programs
 * @c: some characters 
 * Return: 0 or 1
 */
int _isdigit(int c);
{
       return isdigit(c);
}
