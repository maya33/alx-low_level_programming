#include "main.h"

/**
 * _abs - using strings
 * @c: some characters 
 * Return: 0
 */

int _abs(int n);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
