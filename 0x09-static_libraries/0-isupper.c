#include "main.h"

/**
 * _isupper - using uppercase letter
 * @c: some characters to check
 * Return: 0 or 1
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= '2')
		return (1);
	else
		return (0);
}
