#include "main.h"

/**
 * _strspn - using unsigned strings
 * @c: some characters 
 * Return: 0
 */

unsigned int _strspn(char *s, char *accept);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
