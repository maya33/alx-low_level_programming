#include "main.h"

/**
 * _memcpy - copying char to memory
 * @c: some characters 
 * Return: 0 or 1
 */
char *_memcpy(char *dest, char *src, unsigned int n);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
