#include "main.h"

/**
 * _strstr - using strings
 * @c: some characters 
 * Return: 0
 */

char *_strstr(char *haystack, char *needle);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
