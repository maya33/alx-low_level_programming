#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.c>
#include <string.h>
/**
 * _strstr - using strings
 * @c: some characters 
 * Return: 0
 */

char *_strstr(char *haystack, char *needle);
{
      return strstr(haystack, needle);
}
