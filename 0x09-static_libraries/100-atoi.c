#include "main.h"

/**
 * _atoi- using a to i
 * @c: some characters 
 * Return: 0 or 1
 */
int _atoi(char *s);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
