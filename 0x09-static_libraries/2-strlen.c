#include "main.h"

/**
 * _strlen - defining string lenth
 * @c: some characters 
 * Return: 0 or 1
 */
int _strlen(char *s);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
