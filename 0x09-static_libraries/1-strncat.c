#include "main.h"

/**
 * _strncat - string concatination
 * @c: some characters 
 * Return: 0 or 1
 */
char *_strncat(char *dest, char *src, int n);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
