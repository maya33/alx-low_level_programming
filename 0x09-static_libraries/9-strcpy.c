#include "main.h"

/**
 * _strncpy - using strings
 * @c: some characters 
 * Return: 0
 */

char *_strncpy(char *dest, char *src, int n);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
