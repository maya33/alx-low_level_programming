#include "main.h"

/**
 * _isalpha - using alphabetical letters
 * @c: some characters 
 * Return: 0
 */

int _isalpha(int c);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
