#include "main.h"

/**
 * _strcpy - copying some string characters
 * @c: some characters 
 * Return: string
 */

char *_strcpy(char *dest, char *src);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
