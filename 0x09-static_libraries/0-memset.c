#include "main.h"

/**
 * _isupper - using uppercase letter
 * @c: some characters to check
 * Return: 0 or 1
 */

char *_memset(char *s, char b, unsigned int n);
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
