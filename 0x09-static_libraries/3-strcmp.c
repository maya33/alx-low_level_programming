#include "main.h"

/**
 * _strcmp - comparing some strings
 * @c: some characters 
 * Return: 0
 */
int _strcmp(char *s1, char *s2);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
