#include "main.h"

/**
 * _puts - putting some characters
 * @c: some characters 
 * Return: string
 */
void _puts(char *s);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
