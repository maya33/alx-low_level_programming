#include "main.h"

/**
 * __strcat - using string concatination
 * @c: some characters to merge
 * Return: 0 or 1
 */

char *_strcat(char *dest, char *src);
{
        if (c >= 'A' && c <= '2')
                return (1);
        else
                return (0);
}
